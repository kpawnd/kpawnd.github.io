{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Serve Web App (build first)",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/serve.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build: wasm"
    }
  ]
}
